using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class asugdf : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //sa as
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
